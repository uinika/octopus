// let asar = require("asar");

// src = "../octopus";
// dest = "build/app.asar";

// /** 打包完成后的回调函数 */
// callback = () => {
//   console.info("asar打包完成！");
// };

// asar.createPackage(src, dest, callback);
